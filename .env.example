# Database Mode: "local" (SQLite/Prisma) | "supabase" (PostgreSQL via Supabase)
DB_MODE="local"

# Local Database (SQLite via Prisma) - only used when DB_MODE=local
DATABASE_URL="file:./data/indexer.db"

# Supabase (Production) - only used when DB_MODE=supabase
# service_role key is required for write operations
SUPABASE_URL="https://uhjytdjxvfbppgjicfly.supabase.co"
SUPABASE_KEY="sb_secret_xxx"

# Solana RPC
RPC_URL="https://api.devnet.solana.com"
WS_URL="wss://api.devnet.solana.com"

# Program ID (8004 Agent Registry v0.5.0)
PROGRAM_ID="6MuHv4dY4p9E4hSCEPr9dgbCSpMhq8x1vrUexbMVjfw1"

# Indexer Mode: "auto" | "polling" | "websocket"
# auto = tries WebSocket first, falls back to polling if unavailable
INDEXER_MODE="auto"

# Polling Configuration
POLLING_INTERVAL="5000"
BATCH_SIZE="100"

# WebSocket Configuration
WS_RECONNECT_INTERVAL="3000"
WS_MAX_RETRIES="5"

# Logging
LOG_LEVEL="info"
