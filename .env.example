# Database (SQLite by default - zero setup required)
DATABASE_URL="file:./data/indexer.db"

# For PostgreSQL (optional, requires separate installation):
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/indexer8004"

# Solana RPC (works with any provider)
RPC_URL="https://api.devnet.solana.com"
WS_URL="wss://api.devnet.solana.com"

# Program ID (8004 Agent Registry)
PROGRAM_ID="3GGkAWC3mYYdud8GVBsKXK5QC9siXtFkWVZFYtbueVbC"

# Indexer mode: "auto" | "polling" | "websocket"
# auto = tries WebSocket first, falls back to polling if unavailable
INDEXER_MODE="auto"

# Polling interval in milliseconds (default: 5000)
POLLING_INTERVAL="5000"

# Batch size for fetching signatures (default: 100)
BATCH_SIZE="100"

# GraphQL server port
GRAPHQL_PORT="4000"

# Log level: "debug" | "info" | "warn" | "error"
LOG_LEVEL="info"
